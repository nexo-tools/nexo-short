name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, intl, pdo_sqlite
          coverage: none

      - uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}

      - run: composer install --prefer-dist --no-progress --no-interaction

      - name: Composer audit (dependency vulnerabilities)
        run: |
          for i in 1 2 3; do composer audit && exit 0; echo "composer audit attempt $i failed (likely transient network), retrying in 5s"; sleep 5; done
          composer audit

      - name: Pint (code style)
        run: vendor/bin/pint --test

      - name: Larastan (static analysis)
        run: vendor/bin/phpstan analyse --no-progress

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Translations (check in sync)
        run: node scripts/generate-translations.mjs --check

      # The deploy builds assets; with auto-deploy on push, a broken build has to
      # fail here and not on the way to production.
      - name: Build front-end assets
        run: npm run build

      - name: Pest (tests)
        run: |
          cp .env.example .env
          php artisan key:generate
          vendor/bin/pest
